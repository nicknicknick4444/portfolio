from django.contrib import admin
#from .models import SaveQuery

# Register your models here.

# class SaveQueryAdmin(admin.ModelAdmin):
#     fieldsets = [
#         ("session_id", {"fields":["session_id"]}),
#         ("goodness", {"fields":["goodness"]}),
#         ("badness", {"fields":["badness"]}),
#         ]
#     list_display = ("session_id", "goodness", "badness")
# 
# admin.site.register(SaveQuery, SaveQueryAdmin)
